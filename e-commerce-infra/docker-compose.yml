version: '3.8'

services:
  backend:
    build: ../e-commerce-school-supply-api/
    environment:
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
    ports:
      - "8080:8080"

  frontend:
    build: ../e-commerce-school-supply-web/
    ports:
      - "3000:80"